# Copiar a .env en el servidor (opcional) o exportar variables antes de arrancar.
# Windows PowerShell:  $env:PORT="3004"; $env:CORTE_PASSWORD="..."; node server.js
# Linux:               export PORT=3004 CORTE_PASSWORD=... && node server.js

PORT=3000
HOST=0.0.0.0
NODE_ENV=production

# Contraseña para "Limpiar planilla" (cambiar en producción)
CORTE_PASSWORD=Finanzas$2026
